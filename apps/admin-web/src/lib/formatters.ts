// Re-export shared formatters for the admin dashboard.
export {
  formatCurrency,
  discountPercent,
  calcSubtotal,
  formatDate,
  formatDateTime,
  timeAgo,
  formatOrderNumber,
  truncate,
  initials,
} from "@/shared";

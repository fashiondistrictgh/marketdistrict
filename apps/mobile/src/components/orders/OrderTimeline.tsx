import { Text, View } from "react-native";

/**
 * OrderTimeline — placeholder component.
 * Replace with the real implementation when building this feature.
 */
export function OrderTimeline() {
  return (
    <View className="rounded-xl border border-gray-100 bg-white p-4">
      <Text className="text-sm text-gray-500">OrderTimeline</Text>
    </View>
  );
}

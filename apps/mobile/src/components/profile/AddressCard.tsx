import { Text, View } from "react-native";

/**
 * ProfileAddressCard — placeholder component.
 * Replace with the real implementation when building this feature.
 */
export function ProfileAddressCard() {
  return (
    <View className="rounded-xl border border-gray-100 bg-white p-4">
      <Text className="text-sm text-gray-500">ProfileAddressCard</Text>
    </View>
  );
}

// Expo Router provides typed routes via the `typedRoutes` experiment (see app.json).
// This file holds any extra param shapes used with router.push({ pathname, params }).

export type ProductRouteParams = { id: string };
export type CategoryRouteParams = { id: string };
export type OrderRouteParams = { id: string };

// Re-export the shared formatters so screens can import everything from "@/lib/formatters".
export { formatCurrency, discountPercent, calcSubtotal } from "@/shared";
export { formatDate, formatDateTime, timeAgo } from "@/shared";
export { formatOrderNumber, truncate, initials } from "@/shared";
